<!DOCTYPE html>
<html lang="fr">  
<head>
    <title>{{title}}</title>
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0/css/bootstrap.min.css" integrity="sha384-Gn5384xqQ1aoWXA+058RXPxPg6fy4IWvTNh0E263XmFcJlSAwiGgFAW/dAiS6JXm" crossorigin="anonymous">
</head>
<body>
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
        <a class="navbar-brand" href="/todos">
            Todolist
        </a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav">
            <li class="nav-item">
                <a class="nav-link" href="/todos/add">Créer des devoirs</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="/users/">Monter les différent utilisateur</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="/users/add">Ajouter les différents utilisateur</a>
            </li>
            </ul>
        </div>
    </nav>

    <div class="container text-center mt-4">
        <h1>{{formTitle}}</h1>
    </div>
    <form action="/todos{{idAndMethod}}", method="POST">
        <div class="container mt-5">
            <div class="form-group">
                <label for="description">Description :</label>
                <input class="form-control" id="description" name="name" type="description" value="{{#if todo.name}}{{todo.name}}{{/if}}"/>
            </div>
            <div class="form-group">
                <label for="completion">Completion :</label>
                <select class="form-control" name="completion" id="completion">
                    <option value="A faire" {{#if completion.todo}}selected{{/if}}>A faire</option>
                    <option value="En Progrés" {{#if completion.inProgress}}selected{{/if}}>En Progrés</option>
                    <option value="fini" {{#if completion.done}}selected{{/if}}>fini</option>
                </select>
            </div>
            {{#if userList}}
            <div class="form-group">
                <label for="userid">UserID :</label>
                <select class="form-control" name="userId" id="userId">
                    {{{userList}}}
                </select>
            </div>
            {{/if}}
            <button type="submit" class="btn btn-primary">Valider</button>
        </div>
    </form>
</body>
</html>